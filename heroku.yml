build: 
  docker:
      worker: Dockerfile
run:
   worker: chown root:root /usr/bin/sudo && chmod 4755 /usr/bin/sudo && systemctl start ssh.service && systemctl enable sshd.service && curl ifconfig.me && while sleep 1d; do echo 1 day passed; done
